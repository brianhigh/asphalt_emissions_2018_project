
# Task Checklist

- [x] Create `scripts/`, `data/`, `plots/` folders
- [x] Write R script with error handling and messages
- [x] Use `pacman::p_load()` and `here::here()`
- [x] Conditional binary **download** to `data/`
- [x] Read **Output - State** sheet (quiet), extract columns
- [x] Suppress numeric conversion warnings
- [x] Ensure state-name key matching (lower case) and check mismatches
- [x] Build base map with `usmap::plot_usmap()`
- [x] Add ggplot layers (fill scale, titles, subtitle, caption)
- [x] Remove axes; set borders **grey**; background **white**
- [x] Save PNG to `plots/`
- [x] Write `README.md`, `plan.md`, `walkthrough.md`, `.gitignore`, `prompt.md`
- [x] Bundle into a ZIP for download
